import React, { FC } from "react";

import SignupPageComponent from "../_components/signup";

const SignupPage: FC = ({}) => {
  return (
    <div className={`nc-PageSignUp  `}>
      <SignupPageComponent />
    </div>
  );
};

export default SignupPage;
