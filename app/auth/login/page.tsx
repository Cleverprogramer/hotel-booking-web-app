import React, { FC } from "react";
import LoginPageComponent from "../_components/login";

const LoginPage: FC = ({}) => {
  return (
    <div className={`nc-PageLogin`}>
      <LoginPageComponent />
    </div>
  );
};

export default LoginPage;
